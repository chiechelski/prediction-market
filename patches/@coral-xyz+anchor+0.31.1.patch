diff --git a/node_modules/@coral-xyz/anchor/dist/cjs/error.js b/node_modules/@coral-xyz/anchor/dist/cjs/error.js
index d8a4db9..4fd9e1b 100644
--- a/node_modules/@coral-xyz/anchor/dist/cjs/error.js
+++ b/node_modules/@coral-xyz/anchor/dist/cjs/error.js
@@ -223,33 +223,50 @@ function translateError(err, idlErrors) {
     if (features.isSet("debug-logs")) {
         console.log("Translating error:", err);
     }
-    const anchorError = AnchorError.parse(err.logs);
+    let anchorError = null;
+    try {
+        anchorError = AnchorError.parse(err.logs);
+    }
+    catch (_e) {
+        anchorError = null;
+    }
     if (anchorError) {
         return anchorError;
     }
-    const programError = ProgramError.parse(err, idlErrors);
+    let programError = null;
+    try {
+        programError = ProgramError.parse(err, idlErrors);
+    }
+    catch (_pe) {
+        programError = null;
+    }
     if (programError) {
         return programError;
     }
     if (err.logs) {
-        const handler = {
-            get: function (target, prop) {
-                if (prop === "programErrorStack") {
-                    return target.programErrorStack.stack;
-                }
-                else if (prop === "program") {
-                    return target.programErrorStack.stack[err.programErrorStack.stack.length - 1];
-                }
-                else {
-                    // this is the normal way to return all other props
-                    // without modifying them.
-                    // @ts-expect-error
-                    return Reflect.get(...arguments);
-                }
-            },
-        };
-        err.programErrorStack = ProgramErrorStack.parse(err.logs);
-        return new Proxy(err, handler);
+        try {
+            const handler = {
+                get: function (target, prop) {
+                    if (prop === "programErrorStack") {
+                        return target.programErrorStack.stack;
+                    }
+                    else if (prop === "program") {
+                        return target.programErrorStack.stack[target.programErrorStack.stack.length - 1];
+                    }
+                    else {
+                        // this is the normal way to return all other props
+                        // without modifying them.
+                        // @ts-expect-error
+                        return Reflect.get(...arguments);
+                    }
+                },
+            };
+            err.programErrorStack = ProgramErrorStack.parse(err.logs);
+            return new Proxy(err, handler);
+        }
+        catch (_e2) {
+            return err;
+        }
     }
     return err;
 }
diff --git a/node_modules/@coral-xyz/anchor/dist/cjs/provider.js b/node_modules/@coral-xyz/anchor/dist/cjs/provider.js
index 4653c8e..bc793db 100644
--- a/node_modules/@coral-xyz/anchor/dist/cjs/provider.js
+++ b/node_modules/@coral-xyz/anchor/dist/cjs/provider.js
@@ -119,7 +119,12 @@ class AnchorProvider {
                 }
                 else {
                     const logs = (_d = failedTx.meta) === null || _d === void 0 ? void 0 : _d.logMessages;
-                    throw !logs ? err : new web3_js_1.SendTransactionError(err.message, logs);
+                    throw !logs ? err : new web3_js_1.SendTransactionError({
+                        action: "send",
+                        signature: txSig,
+                        transactionMessage: err.message,
+                        logs: logs,
+                    });
                 }
             }
             else {
@@ -189,7 +194,12 @@ class AnchorProvider {
                     }
                     else {
                         const logs = (_c = failedTx.meta) === null || _c === void 0 ? void 0 : _c.logMessages;
-                        throw !logs ? err : new web3_js_1.SendTransactionError(err.message, logs);
+                        throw !logs ? err : new web3_js_1.SendTransactionError({
+                            action: "send",
+                            signature: txSig,
+                            transactionMessage: err.message,
+                            logs: logs,
+                        });
                     }
                 }
                 else {
